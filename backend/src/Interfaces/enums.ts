export enum Source {
  Reddit,
  ArXiv,
  Podcast,
  X,
  News,
}

export enum TopicPeriod {
  "daily",
  "weekly",
  "mothly",
  "yearly",
  "alltime",
}

export enum VoteType {
  UPVOTE = "up",
  DOWNVOTE = "down",
  UNVOTE = "unvote",
}
